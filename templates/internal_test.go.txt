package @@packageName@@_test

import (
	"testing"

	"@@modulePath@@/internal/@@packageName@@"
)

func TestName(t *testing.T) {
	t.Parallel()

	if got := @@packageName@@.Name(); got != "@@projectName@@" {
		t.Errorf("Name() = %q, want %q", got, "@@projectName@@")
	}
}
