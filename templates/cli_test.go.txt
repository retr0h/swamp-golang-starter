package cli_test

import (
	"testing"

	"@@modulePath@@/internal/cli"
)

func TestName(t *testing.T) {
	t.Parallel()

	if got := cli.Name(); got != "@@projectName@@" {
		t.Errorf("Name() = %q, want %q", got, "@@projectName@@")
	}
}

func TestRun(t *testing.T) {
	t.Parallel()

	cli.Run()
}
